# Data directory note

The active dataset currently remains at `Drowsy_dataset/` for compatibility with existing notebook and scripts.

This `data/` directory is kept to align with the MLOps project structure roadmap and can be used for future dataset versioning workflows.
